require('dotenv').config(); const { createClient } = require('@supabase/supabase-js'); const sb = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); (async () => { const { data, error } = await sb.from('disciplines').select('*').limit(1); console.log(data ? 'RLS on disciplines allows SELECT' : error); })();
